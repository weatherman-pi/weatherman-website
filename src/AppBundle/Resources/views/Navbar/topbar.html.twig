<nav class="navbar navbar-default">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path('app_homepage') }}">WeatherMan</a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav navbar-right">
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Sujets<span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        <li><a href="{{ path('app_proposition') }}">Proposition de recherche</a></li>
                        <li><a href="{{ path('app_acquisition_connaissances') }}">Acquisition des connaissances</a></li>
                        <li><a href="{{ path('app_demarche_experimentale') }}">Démarche expérimentale</a></li>
                        <li><a href="{{ path('app_difficultes') }}">Difficultés rencontrées</a></li>
                        <li><a href="{{ path('app_resultats_recherche') }}">Résultats de la recherche</a></li>
                        <li><a href="{{ path('app_conclusion_bilan') }}">Conclusion et bilan</a></li>
                        <li><a href="{{ path('app_bibliographie') }}">Bibliographie et téléchargement</a></li>
                    </ul>
                </li>

                {% if app.user %}
                    <li><a href="{{ path('fos_user_security_logout') }}">{{ app.user.username }}</a></li>
                {% elseif not app.user %}
                    <li><a href="{{ path('fos_user_security_login') }}">Se connecter</a></li>
                {% endif %}
            </ul>
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>